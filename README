1. docker volume create mongodb_data

2. docker run -d --name mongodb_container \
    -p 27017:27017 \
    -v mongodb_data:/data/db \
    -e MONGO_INITDB_ROOT_USERNAME=root \
    -e MONGO_INITDB_ROOT_PASSWORD=example \
    mongo:latest